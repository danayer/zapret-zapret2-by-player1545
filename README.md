# zapret + zapret2 · by Player1545

> Решение для обхода DPI-блокировок на базе [zapret-discord-youtube](https://github.com/Flowseal/zapret-discord-youtube) и [zapret2 / winws2](https://github.com/bol-van/zapret-win-bundle).

Отдельная благодарность [Flowseal](https://github.com/Flowseal) и [bol-van](https://github.com/bol-van) за оригинальные проекты.  
Поддержать оригинального разработчика zapret можно [здесь](https://github.com/bol-van/zapret?tab=readme-ov-file#%D0%BF%D0%BE%D0%B4%D0%B4%D0%B5%D1%80%D0%B6%D0%B0%D1%82%D1%8C-%D1%80%D0%B0%D0%B7%D1%80%D0%B0%D0%B1%D0%BE%D1%82%D1%87%D0%B8%D0%BA%D0%B0).

> **Замедление Telegram?** Читайте в [разделе про Telegram](#про-telegram).

---

## Быстрый старт

1. **Скачайте** архив со страницы [Releases](../../releases).
2. **Разархивируйте** папку в любое удобное место.
3. **Запустите** нужный `.bat` файл от имени администратора.

> **Рекомендация:** используйте `general.bat` или `general(ALT12).bat` — они наиболее стабильны.

Если при запуске появится предупреждение безопасности PowerShell:

```
Security Warning: Script Execution Risk
Invoke-WebRequest parses the content of the web page...
```

Введите `y` или `a` для продолжения.

> **Важно:** программа `winws2` может запросить права администратора — разрешите её запуск. Данная программа имеют открытый исходный код: [zapret2](https://github.com/bol-van/zapret2).

---

## Что такое zapret?

**zapret — это НЕ VPN.**

Он не меняет ваш IP-адрес и не пропускает трафик через иностранные серверы. Это утилита **десинхронизации DPI** (Deep Packet Inspection) на стороне провайдера — она «дурит» систему глубокой проверки пакетов, не затрагивая вашу геолокацию.

---

## Структура проекта

```
zapret-zapret2-by-player1545
 ├── general.bat              — основная стратегия
 ├── general (ALT...).bat     — запасные стратегии
 ├── service.bat              — инструменты управления (удаление сервиса, игровой фильтр и др.)
 ├── bin/                     — бинарные файлы (winws2, фейковые quic и tls)
 ├── lists/                   — листы хостов и IP-адресов
 │   ├── list-general.txt     — основные хосты
 │   ├── list-general-user.txt — пользовательские хосты
 │   ├── ipset-all.txt        — основные IP-диапазоны
 │   ├── list-google.txt      — хосты YouTube
 │   ├── list-exclude.txt     — исключения (DPI не применяется)
 │   ├── list-exclude-user.txt — пользовательские исключения (DPI не применяется)
 │   ├── ipset-exclude.txt    — исключения по IP (DPI не применяется)
 │   ├── ipset-exclude-user.txt — пользовательские исключения по IP (DPI не применяется)
 │   └── ipset-all.txt.backup — резервный ipset с расширенными диапазонами
 └── lua/                     — Lua-скрипты для winws2 (из репозитория zapret2 + кастомные скрипты)
```

---

## Про service.bat

Пункт **1** (установка как сервис) изменён. В оригинальном `service.bat` пункт 1 осуществляет установку zapret как сервис. В данной сборке пункт 1 создаёт автозапуск через **Task Scheduler** с запуском стратегии в фоне.

По умолчанию включён **Game Filter** на режим TCP и UDP. Если он вам не нужен, отключите его через `service.bat`, введя цифру `4`.

---

## Неработающие звонки в Discord

Попробуйте решение из [этого issue](https://github.com/Flowseal/zapret-discord-youtube/issues/8028).

Если звонки всё ещё не работают, смените регион голосового сервера на **Rotterdam**:

- **На сервере:** зайдите в настройки голосового канала → «Назначение региона» → выберите `Rotterdam` (находится под `Japan`). Требуются права на редактирование канала.
- **В личных сообщениях:** нажмите «Другие настройки» (`...` рядом с кнопкой «Отключиться») → «Регион» → `Rotterdam`.

Если и это не помогло — рекомендуется **Cloudflare Warp** или **Внезапный Переезд в Нидерланды**.

---

## Про Telegram

**10.02.2026** — Роскомнадзор начал активно замедлять Telegram: долгая отправка сообщений, медленная загрузка медиа.

**UPD 15.02.2026:** найден способ оживить **веб-версию** Telegram:

1. Скачайте [**последнюю**](https://github.com/Player1545/zapret-zapret2-by-player1545/releases/latest) версию репозитория.
2. В `service.bat` введите `8`.
3. Дополните файл `Hosts` согласно инструкции.

> Для **десктопного приложения** Telegram этот метод не работает.

Для десктопа используйте прокси или утилиту от Flowseal: [tg-ws-proxy](https://github.com/Flowseal/tg-ws-proxy).

---

## Зачем этот форк?

Форк появился потому, что перенести стратегию из `zapret-discord-youtube` в `winws2` (с фильтрацией UDP-портов для игр) оказалось нетривиальной задачей. Различные параметры `fake` и методы десинхронизации не давали результата — игры просто не загружались.

Главное преимущество `zapret2` / `winws2` — расширенная поддержка протоколов для пейлоадов и L7-фильтрации, ради чего и стоит эта возня.
